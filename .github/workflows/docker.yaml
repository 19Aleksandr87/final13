name: doker
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        container: golang:1.24.2
        steps:
          - uses: actions/checkout@v4

          - name: Run Unit Tests
            run: go test -run ^TestApp$ ./tests
            

