# Файлы для итогового задания

В директории `tests` находятся тесты для проверки API, которое должно быть реализовано в веб-сервере.

Директория `web` содержит файлы фронтенда.

Описание проекта — для чего он создан, что он делает.
Планировщик задач. Предназначен для составления распмсания ежедневных задач.

Список выполненных заданий со звёздочкой. Если их нет, напишите, что задания повышенной трудности не выполнялись.
1. Реализуйте возможность определять извне порт при запуске сервера. Если существует переменная окружения TODO_PORT, сервер при старте должен слушать порт со значением этой переменной. 
2. Реализуйте возможность определять путь к файлу базы данных через переменную окружения. 
3. Вычисление следующих дат с указанием недель и месяцев
4. Фильтрация задач по дате и подстроке

Инструкция по запуску кода локально: дополнительные флаги, примеры .env и так далее. Напишите, какой адрес следует указывать в браузере.
Инструкция по запуску кода локально: "go run main.go"
адрес в браузере "7540"

Инструкция по запуску тестов. Укажите, какие параметры в tests/settings.go следует использовать.
package tests
var Port = 7540
var DBFile = "../pkg/db/scheduler.db"
var FullNextDate = true
var Search = true
var Token = ``

